@extends('layouts.app')

@section('content')
<div class="container ">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card text-center text-uppercase text-primary mb-0 navbar-brand js-scroll-trigger font-weight-bold">
                <div class="card-header text-center text-uppercase text-primary mb-0 navbar-brand js-scroll-trigger font-weight-bold">SE CONNECTER</div>

                <div class="card-body text-center text-uppercase text-primary mb-0 navbar-brand js-scroll-trigger font-weight-bold">
                    <form method="POST" action="{{ route('login') }}">
                        @csrf

                        <div class="form-group row">
                            <label for="email" class="col-md-4 col-form-label text-md-right text-center text-uppercase text-primary mb-0 navbar-brand js-scroll-trigger font-weight-bold">{{ __('E-Mail Address') }}</label>

                            <div class="col-md-6">
                                <input id="email" type="email" class="form-control @error('email') is-invalid @enderror" name="email" value="{{ old('email') }}" required autocomplete="email" autofocus>

                                @error('email')
                                    <span class="invalid-feedback" role="alert">
                                        <strong>email ou password no valide</strong>
                                    </span>
                                @enderror
                            </div>
                        </div>

                        <div class="form-group row">
                            <label for="password" class="col-md-4 col-form-label text-md-right text-center text-uppercase text-primary mb-0 navbar-brand js-scroll-trigger font-weight-bold">{{ __('Password') }}</label>

                            <div class="col-md-6">
                                <input id="password" type="password" class="form-control @error('password') is-invalid @enderror" name="password" required autocomplete="current-password">

                                @error('password')
                                    <span class="invalid-feedback" role="alert">
                                        <strong>email ou password no valide</strong>
                                    </span>
                                @enderror
                            </div>
                        </div>

                        <div class="form-group row">
                            <div class="col-md-6 offset-md-4">
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" name="remember" id="remember" {{ old('remember') ? 'checked' : '' }}>

                                    <label class="form-check-label text-center text-uppercase text-primary mb-0 navbar-brand js-scroll-trigger font-weight-bold" for="remember">
                                        {{ __('Remember Me') }}
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div class="form-group row mb-0">
                            <div class="col-md-8 offset-md-4">
                                <button type="submit" class="btn btn-primary text-uppercase font-weight-bold">
                                    SE CONNECTER
                                </button>

                                @if (Route::has('password.request'))
                                    <a class="btn btn-link text-uppercase font-weight-bold" href="{{ route('password.request') }}">
                                        {{ __('MOT DE PASSE OUBLIES ?') }}
                                    </a>
                                @endif
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
